namespace GalacticLauncher.Core;

// Represents enums used in the database
public enum VersionType { Alpha, Beta, Release, Snapshot }
public enum Platform { Windows, Linux, MacSilicon, MacIntel }
public enum AlertLevel { Stable, Alert, Danger }
public enum ImageType { Icon, Screenshot, Banner }
