namespace GalacticLauncher.Core;

// Represents enums used in the database
public enum VersionType { Unknown, Alpha, Beta, Release, Snapshot }
public enum Platform { Unknown, Windows, Linux, MacSilicon, MacIntel }
public enum AlertLevel { Unknown, Stable, Alert, Danger }
public enum ImageType { Unknown, Icon, Screenshot, Banner }
